# Dependencias del Agente RAG
# Instalar con: pip install -r requirements.txt

# LangChain core
langchain>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0

# Base vectorial local
chromadb>=0.5.0

# Embeddings locales (descarga ~90 MB en primer uso)
sentence-transformers>=3.0.0

# Lectura de PDFs
pypdf>=4.0.0

# LLM local (conexión HTTP a Ollama)
httpx>=0.27.0

# LLM en la nube (OpenAI / DeepSeek)
openai>=1.30.0
